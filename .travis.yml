dist: trusty
sudo: required
language: python
cache: pip
services:
  - postgresql
  - redis-server
addons:
  postgresql: "10"
  apt:
    packages:
    - postgresql-10
    - postgresql-client-10
python:
    - 3.6.5
env:
  global:
      - DOCKER_DRIVER=overlay2
      - DJANGO=2.0.8
      - ALLOWED_HOSTS=*
      - DEBUG=True
      - PRODUCTION=False
      - REDIS_URL=redis://localhost:6379
      - SECRET_KEY=jumpcut
<<<<<<< HEAD
      - DATABASE_URL=postgres://postgres:postgres@localhost:5433/travis_ci
      - TESTING_DATABASE_URL=postgres://postgres:postgres@localhost:5433/postgres
=======
      - DATABASE_URL=postgres://postgres:postgres@localhost:5433/travis
      - TESTING_DATABASE_URL=postgres://postgres:postgres@localhost:5433/travis_ci
>>>>>>> 66cddcfea0e8e7195bd17f13a833e9ced770e87f
      - WSGI_APPLICATION=jumpcut.www_wsgi.application
      - PGPORT=5433
before_install:
   - sudo sed -i -e '/local.*peer/s/postgres/all/' -e 's/peer\|md5/trust/g' /etc/postgresql/*/main/pg_hba.conf
   - sudo service postgresql restart
   - sleep 1
install:
   - pip install -r ./backend/requirements.txt
   - pip install -r ./backend/testing_requirements.txt
   - pip install codecov
before_script:
   - psql -c 'create database travis_ci;' -U postgres
script:
   - cd backend
   - invoke run-python-linter
   - src/manage.py migrate  
   - invoke foundation
   - invoke fixtures
   - invoke run-python-tests --coverage
after_success:
   - codecov --token=$CODECOV_TOKEN
   - docker login -u $DOCKER_USER -p $DOCKER_PASS
   - export REPO=streisandjumpcut/streisand
   - export TAG=latest
   - cd ../frontend
   - cp env.template .env
   - docker build -f ./production/Dockerfile -t $REPO:$TAG .
   - cd ../backend
   - docker build -f ./production/Dockerfile -t $REPO:$TAG .
   - docker tag $REPO:$TAG
   - docker push $REPO
